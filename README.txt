Steps to get the server up and running:

1. Install Node

2. Run 
	$ npm install --dev

3. Create "dev" file in main directory

//4. Edit diet-ect to correctly render different pages.
//	change the != to ===
// 	only needed if you're using ect templating.

5. Run server with "node index.js" or "node ." in the main directory